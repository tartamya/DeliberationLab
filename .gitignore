# Secrets -- never commit real API keys
config/secrets.R

# Saved deliberation sessions (may be large / private)
sessions/

# R artifacts
.Rproj.user/
.Rhistory
.RData
.RDataTmp
.Ruserdata
Rplots.pdf
*.rds

# API keys (plain text, hidden folder) -- never commit
.secrets/

# Windows shortcuts (machine-specific)
*.lnk

# Automatic backups written before a destructive role-library edit
config/roles.backup-*.json

# Working drafts and exported images in the project root -- local scratch, not
# source. Deliberation_Lab_Framework.docx is tracked deliberately (see below).
/AI_Debate_*.docx
/Multi_AI_Debate_*.docx
/Multi_AI_Debate_*.pdf
/img-*.jpg
/preview-*.jpg
